.. _multiple_images:

Multiple images
===============

This code uses the ``html-keyboard-response`` plugin, so make sure it's included in your
``experiment.html``.

.. code:: javascript

{{ multiple_images_multiple_images_js }}